<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:core="clr-namespace:GOKDOGANIHA.UI.Core">

    <!-- Flat tactical tab style: transparent, cyan underline on selected, subtle hover.
         Use on a plain TabItem via Style="{StaticResource TacticalTab}".
         IconTabItem below picks this up automatically via BasedOn. -->
    <Style x:Key="TacticalTab" TargetType="TabItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource TacticalTextSecondary}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="22,12" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Bd"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0,0,0,2"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#11FFFFFF" />
                            <Setter Property="Foreground" Value="{StaticResource TacticalTextPrimary}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush"
                                    Value="{StaticResource TacticalAccent}" />
                            <Setter Property="Foreground" Value="{StaticResource TacticalAccent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Implicit style for IconTabItem — no key, applies automatically to every
         IconTabItem in the app so you never have to Style= anything. -->
    <Style TargetType="{x:Type core:IconTabItem}" BasedOn="{StaticResource TacticalTab}" />

</ResourceDictionary>
