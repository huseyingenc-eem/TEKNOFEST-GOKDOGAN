<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
                    xmlns:core="clr-namespace:GOKDOGANIHA.UI.Core">

    <Style TargetType="{x:Type core:OverlayHost}">
        <!-- Visibility is driven imperatively in OverlayHost.OnIsOpenChanged -->
        <Setter Property="Focusable" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type core:OverlayHost}">
                    <Grid>
                        <!-- Dim backdrop (dismisses when clicked if DismissOnBackdropClick=true) -->
                        <Rectangle x:Name="PART_Backdrop" Fill="{StaticResource TacticalBackdrop}" />

                        <!-- Floating content surface -->
                        <Border Background="{StaticResource TacticalGlassStrong}"
                                BorderBrush="{StaticResource TacticalOverlayBorder}"
                                BorderThickness="1"
                                CornerRadius="14"
                                Margin="{TemplateBinding ContentMargin}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Direction="270"
                                                  ShadowDepth="8" BlurRadius="40" Opacity="0.75" />
                            </Border.Effect>

                            <!-- Inner glass highlight (matches IconButton/PanelFrame style) -->
                            <Grid>
                                <Border BorderBrush="{StaticResource TacticalOverlayHighlight}"
                                        BorderThickness="1,1,1,0"
                                        CornerRadius="13"
                                        Margin="1"
                                        IsHitTestVisible="False" />

                                <DockPanel>
                                    <!-- Header row -->
                                    <Border DockPanel.Dock="Top"
                                            BorderBrush="{StaticResource TacticalOverlayBorder}"
                                            BorderThickness="0,0,0,1"
                                            Padding="20,12,12,12">
                                        <Border.Visibility>
                                            <Binding Path="ShowHeader"
                                                     RelativeSource="{RelativeSource TemplatedParent}"
                                                     Converter="{x:Static materialDesign:BooleanToVisibilityConverter.CollapsedInstance}" />
                                        </Border.Visibility>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" Orientation="Horizontal"
                                                        VerticalAlignment="Center">
                                                <materialIcons:MaterialIcon
                                                    Kind="{TemplateBinding HeaderIconKind}"
                                                    Width="18" Height="18"
                                                    Foreground="{StaticResource TacticalAccent}"
                                                    VerticalAlignment="Center" />
                                                <TextBlock Text="{TemplateBinding Title}"
                                                           FontSize="14" FontWeight="SemiBold"
                                                           Foreground="{StaticResource TacticalTextPrimary}"
                                                           Margin="10,0,0,0"
                                                           VerticalAlignment="Center" />
                                                <TextBlock Text="{TemplateBinding Subtitle}"
                                                           FontSize="11"
                                                           FontFamily="Consolas"
                                                           Foreground="{StaticResource TacticalTextSecondary}"
                                                           Margin="14,0,0,0"
                                                           VerticalAlignment="Center" />
                                            </StackPanel>

                                            <Button Grid.Column="2"
                                                    x:Name="PART_CloseButton"
                                                    ToolTip="Kapat (Esc)"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="6"
                                                    Cursor="Hand"
                                                    Focusable="False">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="BtnBg"
                                                                Background="Transparent"
                                                                CornerRadius="4"
                                                                Padding="6">
                                                            <materialIcons:MaterialIcon Kind="Close"
                                                                Width="18" Height="18"
                                                                Foreground="{StaticResource TacticalTextSecondary}" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="BtnBg" Property="Background"
                                                                        Value="#22FFFFFF" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </Grid>
                                    </Border>

                                    <!-- Inner content -->
                                    <ContentPresenter
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                </DockPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
