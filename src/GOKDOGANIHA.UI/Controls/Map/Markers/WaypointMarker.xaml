<UserControl x:Class="GOKDOGANIHA.UI.Controls.Map.Markers.WaypointMarker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Name="Self"
             mc:Ignorable="d"
             Width="28" Height="28"
             IsHitTestVisible="False">

    <Grid>
        <!-- Dış halka — ipucu hat'ından farklı görünür -->
        <Ellipse Stroke="#FF00D1C7" StrokeThickness="2"
                 Fill="#1A00D1C7" Width="28" Height="28">
            <Ellipse.Effect>
                <DropShadowEffect Color="#FF00D1C7" BlurRadius="8"
                                  ShadowDepth="0" Opacity="0.5" />
            </Ellipse.Effect>
        </Ellipse>
        <!-- İç bölge -->
        <Ellipse Fill="#CC0A1620" Width="22" Height="22" />
        <!-- Numara -->
        <TextBlock Text="{Binding WaypointIndex, ElementName=Self}"
                   FontFamily="Consolas" FontSize="11" FontWeight="Bold"
                   Foreground="#FF00D1C7"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />
    </Grid>
</UserControl>
