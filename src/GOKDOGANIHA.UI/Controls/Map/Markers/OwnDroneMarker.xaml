<UserControl x:Class="GOKDOGANIHA.UI.Controls.Map.Markers.OwnDroneMarker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             Width="36" Height="36"
             RenderTransformOrigin="0.5,0.5">
    <UserControl.RenderTransform>
        <RotateTransform x:Name="Heading" Angle="0" />
    </UserControl.RenderTransform>
    <Grid>
        <Ellipse Width="32" Height="32"
                 Fill="{StaticResource TacticalAccent}"
                 Opacity="0.25" />
        <materialIcons:MaterialIcon Kind="AirplaneMarker"
                                    Width="24" Height="24"
                                    Foreground="{StaticResource TacticalAccent}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
    </Grid>
</UserControl>
