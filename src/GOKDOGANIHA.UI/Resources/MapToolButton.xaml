<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Map toolbar buton stili — glass + tactical icons.
         Global resource (App.xaml'da merge) — MapPanel + MainWindow Map FAB popover
         birlikte kullanır. DRY: tek style, çoklu yer. -->
    <Style x:Key="MapToolButton" TargetType="Button">
        <Setter Property="Background" Value="#B30A1620" />
        <Setter Property="BorderBrush" Value="{StaticResource TacticalOverlayBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,5" />
        <Setter Property="Foreground" Value="{StaticResource TacticalTextSecondary}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#CC0A1620" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource TacticalAccent}" />
                            <Setter Property="Foreground" Value="{StaticResource TacticalAccent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
